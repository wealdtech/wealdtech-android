package com.wealdtech.android.providers;

/**
 */
public enum ConfigurationState
{
  /**
   * The provider is not configured to provide data
   */
  NOT_CONFIGURED,
  /**
   * The provider is configured to provide data
   */
  CONFIGURED
}
